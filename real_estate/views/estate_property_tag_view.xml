<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- Search View -->
    <record id="estate_property_tag_search_view" model="ir.ui.view">
        <field name="name">estate.property.tag.search.view</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <search string="Estate Tag">
                <field name="name"/>
            </search>
        </field>
    </record>

    <!-- Tree View -->
    <record id="estate_property_tag_tree_view" model="ir.ui.view">
        <field name="name">estate.property.tag.view.tree</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <list string="Estate Tag">
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </list>
        </field>
    </record>

    <record id="estate_property_tag_form_view" model="ir.ui.view">
        <field name="name">estate.property.tag.view.form</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <form string="Estate Form">
                <group>
                    <group>
                        <field name="name"/>
                    </group>
                    <group>
                        <field name="color" widget="color_picker"/>
                    </group>
                </group>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Real Estate Tag</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
